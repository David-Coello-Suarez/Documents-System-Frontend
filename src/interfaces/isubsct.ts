export interface isubsct {
  locali_locali: number
  locali_descri: string

  sector_sector: number
  sector_nombre: string

  subsct_subsct: number
  subsct_nombre: string
  subsct_status: number

  subsct_usucre: string
  subsct_feccre: string
  subsct_usuact: string
  subsct_fecact: string
}
